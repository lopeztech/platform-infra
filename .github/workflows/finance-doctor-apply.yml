name: finance-doctor — Apply

on:
  push:
    branches:
      - master
    paths:
      - 'projects/finance-doctor/**'
      - 'modules/**'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  apply:
    runs-on: ubuntu-latest
    env:
      TF_DIR: projects/finance-doctor
      TF_ENV: prod
    steps:
      - uses: actions/checkout@v6

      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "~1.6"

      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.FINANCE_DOCTOR_WIF_PROVIDER }}
          service_account: ${{ secrets.FINANCE_DOCTOR_SA_EMAIL }}

      - name: Terraform Init
        run: |
          terraform -chdir=${{ env.TF_DIR }} init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="prefix=terraform/state/finance-doctor/${{ env.TF_ENV }}" \
            -input=false

      - name: Terraform Apply
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          terraform -chdir=${{ env.TF_DIR }} apply \
            -var-file="environments/${{ env.TF_ENV }}/terraform.tfvars" \
            -var="billing_account=${{ secrets.BILLING_ACCOUNT_ID }}" \
            -input=false \
            -auto-approve

  verify:
    needs: apply
    runs-on: ubuntu-latest
    env:
      PROJECT_ID: finance-doctor-lcd
      REGION: australia-southeast1
    steps:
      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.FINANCE_DOCTOR_WIF_PROVIDER }}
          service_account: ${{ secrets.FINANCE_DOCTOR_SA_EMAIL }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Verify infrastructure
        run: |
          set -euo pipefail
          PASS=0; FAIL=0; ERRORS=()

          check() {
            local label="$1"; shift
            if "$@" &>/dev/null; then
              echo "  ✅ $label"; ((PASS++)) || true
            else
              echo "  ❌ $label"; ((FAIL++)) || true; ERRORS+=("$label")
            fi
          }

          echo "── APIs ──"
          for api in iam.googleapis.com run.googleapis.com \
                     secretmanager.googleapis.com monitoring.googleapis.com \
                     people.googleapis.com; do
            check "API: $api" \
              gcloud services list --project="$PROJECT_ID" --filter="config.name=$api" --format="value(state)"
          done

          echo "── Service Accounts ──"
          check "SA: finance-doctor-functions (Callables runtime)" \
            gcloud iam service-accounts describe "finance-doctor-functions@${PROJECT_ID}.iam.gserviceaccount.com" --project="$PROJECT_ID" --format="value(email)"

          echo "── Callable Functions (Cloud Run v2 backing services) ──"
          for fn in advicechat dashboardtips taxadvice investmentsadvice expensesimport expensesmigrate expensesreanalyse; do
            check "Callable: $fn" \
              gcloud run services describe "$fn" --project="$PROJECT_ID" --region="$REGION" --format="value(name)"
          done

          echo "── Firestore ──"
          check "Firestore database exists" \
            gcloud firestore databases describe --project="$PROJECT_ID" --format="value(name)"

          echo ""
          echo "════════════════════════════════════════"
          echo "  Passed: $PASS  Failed: $FAIL"
          if [[ ${#ERRORS[@]} -gt 0 ]]; then
            echo "  Failures:"
            for e in "${ERRORS[@]}"; do echo "    - $e"; done
          fi
          echo "════════════════════════════════════════"
          [[ $FAIL -eq 0 ]]
