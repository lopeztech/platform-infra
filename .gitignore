# Terraform
.terraform/
*.tfstate
*.tfstate.backup
# Keep .terraform.lock.hcl committed — it pins provider versions for CI
*.tfvars.json
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
